/* OriTV TV navigation — adapted from Lampa Controller/Navigator architecture */
(()=>{'use strict';
const q=s=>document.querySelector(s),qa=s=>[...document.querySelectorAll(s)];let focused=null,zone='nav',navIndex=0;
const visible=e=>{if(!e||e.disabled)return false;const r=e.getBoundingClientRect(),c=getComputedStyle(e);return c.display!=='none'&&c.visibility!=='hidden'&&r.width>0&&r.height>0};
const navItems=()=>qa('.brand,.nav-item,.icon-btn').filter(visible),contentItems=()=>qa('#view .focusable').filter(visible),rows=()=>qa('#view .row').filter(visible);
function focus(el){if(!visible(el))return;qa('.focused').forEach(x=>x.classList.remove('focused'));focused=el;el.classList.add('focused');try{el.scrollIntoView({block:'nearest',inline:'nearest'})}catch(_){} }
function focusNav(i=navIndex){const a=navItems();if(!a.length)return;zone='nav';navIndex=Math.max(0,Math.min(i,a.length-1));focus(a[navIndex])}
function nearest(from,list,dir){if(!from)return null;const r=from.getBoundingClientRect(),cx=r.left+r.width/2,cy=r.top+r.height/2;let best=null,score=Infinity;for(const el of list){if(el===from)continue;const x=el.getBoundingClientRect(),ex=x.left+x.width/2,ey=x.top+x.height/2,p=dir==='left'?cx-ex:dir==='right'?ex-cx:dir==='up'?cy-ey:ey-cy;if(p<5)continue;const cross=(dir==='left'||dir==='right')?Math.abs(ey-cy):Math.abs(ex-cx),s=p+cross*2.2;if(s<score){score=s;best=el}}return best}
function move(dir){const cur=focused||q('.focused')||contentItems()[0];if(!cur)return;if(zone==='nav'){if(dir==='left')return focusNav(navIndex-1);if(dir==='right')return focusNav(navIndex+1);if(dir==='down'){const f=contentItems()[0];if(f){zone='content';focus(f)}return}return}if(dir==='left'||dir==='right'){const box=cur.closest('.cards'),list=box?[...box.querySelectorAll('.focusable')].filter(visible):contentItems(),n=nearest(cur,list,dir);if(n)focus(n);return}const row=cur.closest('.row');if(row){const rs=rows(),ri=rs.indexOf(row),ti=dir==='down'?ri+1:ri-1;if(ti>=0&&ti<rs.length){const list=[...rs[ti].querySelectorAll('.focusable')].filter(visible),n=nearest(cur,list,dir)||list[0];if(n)return focus(n)}if(dir==='up'&&ri===0){const h=qa('.hero .focusable').find(visible);if(h)return focus(h)}}if(dir==='up'){const h=qa('.hero .focusable').find(visible);if(h&&cur!==h)return focus(h);focusNav(navIndex)}else if(dir==='down'){const f=contentItems()[0];if(f)focus(f)}}
function activate(){const el=focused||q('.focused');if(el)el.click()}
function back(){const p=q('.oritv-player');if(p&&window.OriTVCore?.closePlayer){window.OriTVCore.closePlayer();return}const d=q('#detail'),s=q('#search');if(d&&!d.classList.contains('hidden')){if(typeof closeDetail==='function')closeDetail();else d.classList.add('hidden');return}if(s&&!s.classList.contains('hidden')){if(typeof closeSearch==='function')closeSearch();else s.classList.add('hidden');return}if(typeof goBack==='function'){goBack();return}history.back()}
function key(e){return({37:'left',38:'up',39:'right',40:'down',13:'enter',461:'back',27:'back',8:'back'})[e.keyCode||e.which]}
function bind(){document.addEventListener('focusin',e=>{if(e.target.classList.contains('focusable')){focused=e.target;zone=e.target.closest('#view')?'content':'nav'}});document.addEventListener('keydown',e=>{const k=key(e);if(!k)return;if(!visible(focused)){const f=q('.focused')||navItems()[0]||contentItems()[0];if(f)focus(f)}if(k==='left'||k==='right'||k==='up'||k==='down'){e.preventDefault();e.stopImmediatePropagation();move(k);return}if(k==='enter'){e.preventDefault();e.stopImmediatePropagation();activate();return}if(k==='back'){e.preventDefault();e.stopImmediatePropagation();back()}},{capture:true});new MutationObserver(()=>{if(!visible(focused)){const f=q('.focused')||navItems()[0]||contentItems()[0];if(f)focus(f)}}).observe(document.body,{subtree:true,childList:true});setTimeout(()=>{const f=q('.focused')||navItems()[0];if(f)focus(f)},50)}
window.OriTVNavigation={focus,activate,move,back,nearest,current:()=>focused,items:()=>qa('.focusable').filter(visible)};window.OriTVLampaNavigation=window.OriTVNavigation;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind);else bind();
})();